{% extends 'base.html.twig' %}

{% block body %}
    <div class="form">
        {{ form(form) }}
    </div>
    <div>
        <a href="{{ path('users') }}"
           class="list">Back to all users</a><br>
    </div>
{% endblock %}